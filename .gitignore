.DS_Store
.claude/
local/
config/host-redirects.yaml
docker-compose.infrastructure.yml

# Private plugins (contain inline config / secrets)
plugins/custom/
scripts/widget-push.sh
scripts/widget.sh

# Stage cockpit — kommt zu einem späteren Zeitpunkt als Teil eines
# Plugin-Bundle-Releases. Timing offen (länger als ursprünglich geplant).
# Bis dahin nur lokal/Synology.
plugins/core/55-stage.rb
plugins/core/stage/
config/stage.yaml

# Runtime data (generated by scripts) — Verzeichnis selbst bleibt via .gitkeep
data/*
!data/.gitkeep
data/cheatsheet/
